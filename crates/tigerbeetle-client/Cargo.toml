[package]
name = "tigerbeetle-client"
version = "0.16.67"
edition = "2021"
description = "TigerBeetle client for Rust — pre-built static library for all supported platforms"
license = "Apache-2.0"
authors = ["TigerBeetle, Inc", "long.tpt@newera.inc"]
homepage = "https://tigerbeetle.com"
repository = "https://github.com/tigerbeetle/tigerbeetle"
documentation = "https://docs.rs/tigerbeetle-client"
keywords = ["finance", "accounting", "ledger", "database", "transactions"]
categories = ["finance", "database"]
rust-version = "1.63"
# Need to do this to pick up the assets directory and exclude zig files
include = [
  "build.rs",
  "src/*.rs",
  "assets/tb_client.h",
  "assets/lib/*/{*.a,*.lib}",
]

[dependencies]
bitflags = "2.6.0"
futures-channel = "0.3.31"

[build-dependencies]
anyhow = "1.0.93"

[dev-dependencies]
anyhow = "1.0.93"
futures = "0.3.31"
